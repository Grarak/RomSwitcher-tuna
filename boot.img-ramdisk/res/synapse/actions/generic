if [ -f $1 ];then
	if [[ ! -z $2 ]]; then
		echo $2 > $1
		if [ "$1" == "/sys/module/panel_s6e8aa0/parameters/contrast" ]; then
			echo `cat /sys/class/misc/colorcontrol/multiplier` > /sys/class/misc/colorcontrol/multiplier
		fi
	fi

	echo `cat $1`
fi
